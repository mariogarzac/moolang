var float y,z;
var int a,b, d, c, f, g, h , i;
var file arch;
var int x;
var char key, md5, sha;

main(){

   key = generate_key()
   if (x -lt 1){
       key = generate_key()
       hash_md5(arch)
   
      }
       else{
           encrypt(arch, key)
           hash_sha256(arch)
           hash_md5(arch)
           decrypt("asdfasii00b", key)
           }

   if (x -ge 10){
       key = generate_key()
       hash_md5(arch)
   
      }

    if (z -lt y){
        print("hi")
    }else{
        print("hello")
        }
}




