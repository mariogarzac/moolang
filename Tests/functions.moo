
var float x,y;
var float i;


func funcDos(float a, float b) -> float {
    if (x -lt b){
         x = 10 /2 * 5 + y - 4;
        }
         return x;
    }

# func funcTres(float a, float b) -> float {
#     if (a -lt b){
#             x = y + b * a /3;
#         }else{
#                 while(a -gt b){
#                         a = a + 1;
#                     }
#             }
#     return 0.0;
#     }

main(){
    y = funcDos(x,y)

}
