
var int i,x;
var float y;


func funcDos(float a) -> int {
    y = x + y / 2;
         return x;
    }

main(){
    var float p;

    p = x + y / 2;
    i = funcDos(y);

}

# func funcTres(float a, float b) -> float {
#     if (a -lt b){
#             x = y + b * a /3;
#         }else{
#                 while(a -gt b){
#                         a = a + 1;
#                     }
#             }
#     return 0.0;
#     }

