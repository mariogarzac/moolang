var float y,z;
 var int a,b, d, c, f, g, h , i;
var file arch;
var int x;
var char key, md5, sha;

main(){

  if (x -lt 1){
      key = generate_key()
      hash_md5(arch)
  
     }
      else{
          encrypt(arch, key)
          hash_sha256(arch)
          hash_md5(arch)
          decrypt("asdfasii00b", key)
          }

  while(x -lt 10 and 10 -gt y){
         y = 1 + 3 /2 * 5 + (10 /2);
         generate_key()
     }
}




